# Local Supabase from locs_server (`npx supabase start`), seeded with test
# accounts; see locs_server/supabase/seed.sql. Log in as tester@locs.test /
# locs-test-1234.
#
# 10.0.2.2 is the host machine as seen from the Android emulator. For the iOS
# simulator use 127.0.0.1; for a phone, this machine's LAN address.
#
# These are the Supabase CLI's fixed local-development values, not secrets.
EXPO_PUBLIC_SUPABASE_URL=http://10.0.2.2:54321
EXPO_PUBLIC_SUPABASE_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH
